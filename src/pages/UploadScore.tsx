import UploadScore from '../components/UploadScore/UploadScore';

const UploadScoreForm = () => {
  return (
    <div style={{ 
      padding: '0.75rem',
      marginTop: '0rem'
    }}>
      <UploadScore />
    </div>
  );
};

export default UploadScoreForm;